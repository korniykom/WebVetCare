spring:
  application:
    name:
      auth-service
  datasource:
    url: jdbc:postgresql://192.168.39.27:30432/authdb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
app:
  jwt:
    access-token-expirty: 60
    refresh-token-expiry: 120
logging:
  level:
    org.flywaydb: DEBUG