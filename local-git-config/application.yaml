spring:
  application:
    name: webvetcare

logging:
  level:
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include: refresh, health, info
  endpoint:
    health:
      show-details: always