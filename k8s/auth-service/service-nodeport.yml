apiVersion: v1
kind: Service
metadata:
  name: auth-postgres-debug
  namespace: webvetcare
spec:
  type: NodePort
  selector:
    app: auth-postgres
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 30432